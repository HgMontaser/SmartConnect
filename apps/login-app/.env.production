# Used only by `npm run build` (Vite's production mode) — local dev (`npm run dev`) keeps
# using .env with docker-compose's localhost URLs. This is what gets baked into the
# container image deployed to OKE.
VITE_KEYCLOAK_URL=http://keycloak.51.170.140.178.nip.io
VITE_BUSINESS_APP_HOME_URL=http://app.51.170.140.178.nip.io/
