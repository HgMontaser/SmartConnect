server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — any path that isn't a real static file falls back to index.html so
    # client-side routing (and a hard refresh mid-flow) doesn't 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
